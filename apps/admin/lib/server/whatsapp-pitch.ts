/**
 * @deprecated moved to `@/lib/whatsapp-pitch` — this file is pure text logic
 * with no server-only dependencies, so both the client dialog and server
 * actions can import it directly. Kept as a re-export for any stale imports.
 */
export * from "../whatsapp-pitch";
